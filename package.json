{
  "name": "03-bases-node",
  "version": "0.0.1",
  "description": "Es un programa que crea un archivo con la tabla de multiplicacion del valor asignado por consola",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "base3": "node app --base=3"
  },
  "author": "Will CD",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.4.0",
    "yargs": "^17.5.1"
  }
}
